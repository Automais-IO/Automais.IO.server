# ===== BANCO DE DADOS =====
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_NAME=automais_iot
DATABASE_USER=postgres
DATABASE_PASSWORD=postgres

# String de conexão completa (alternativa)
# DATABASE_CONNECTION=Host=localhost;Port=5432;Database=automais_iot;Username=postgres;Password=postgres

# ===== CHIRPSTACK =====
# Servidor onde o ChirpStack está rodando
CHIRPSTACK_API_URL=http://srv01.automais.io:8080

# Token de autenticação do ChirpStack
# Para gerar: 
# 1. Acesse http://srv01.automais.io:8080
# 2. Faça login
# 3. Vá em "API Keys" e crie um novo token
CHIRPSTACK_API_TOKEN=eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.SEU_TOKEN_AQUI

# ===== EMQX =====
# Servidor MQTT
EMQX_BROKER_URL=srv01.automais.io
EMQX_BROKER_PORT=1883
EMQX_WEBSOCKET_PORT=8083

# Credenciais EMQX (se necessário autenticação)
EMQX_USERNAME=admin
EMQX_PASSWORD=public

# Dashboard EMQX: http://srv01.automais.io:18083
# Usuário padrão: admin / public

# ===== APLICAÇÃO =====
ASPNETCORE_ENVIRONMENT=Development
ASPNETCORE_URLS=http://localhost:5000

# Chave para serviços internos (routeros, vpnserver)
# Gere com: openssl rand -base64 32
# Os serviços Python enviam o mesmo valor no header X-Automais-Internal-Key
# Se não definir, chamadas dos serviços sem Bearer JWT retornam 403
AUTOMAIS_INTERNAL_API_KEY=

# ===== OBSERVAÇÕES =====
# 1. Copie este arquivo para .env e preencha os valores reais
# 2. NUNCA commite o arquivo .env no Git
# 3. O .env é ignorado pelo .gitignore

